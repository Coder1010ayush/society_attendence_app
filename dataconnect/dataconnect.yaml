specVersion: "v1"
serviceId: "attendenceapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "attendenceapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
